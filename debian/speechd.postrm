#!/bin/sh -e

#DEBHELPER#

USER_NAME=speechd

if test "$1" = "purge"; then
  rm -rf /var/log/speechd
  
  if id -u $USER_NAME >/dev/null 2>&1; then
    deluser --quiet --remove-home $USER_NAME
  fi
fi
