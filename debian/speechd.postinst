#!/bin/sh -e

USER_NAME=speechd

if ! id -u $USER_NAME >/dev/null 2>&1; then
  adduser --quiet --system --home /var/run/speechd --ingroup audio \
          --shell /bin/sh --disabled-login \
	  --gecos 'Speech Dispatcher' $USER_NAME
fi
chown $USER_NAME /var/log/speechd

#DEBHELPER#
