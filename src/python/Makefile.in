
prefix = @prefix@
datadir = @datadir@
datarootdir = @datarootdir@

SPD_CONF_PATH_PY="speechd_config/spd_conf_path.py"

all:

install:
	if test -x /usr/bin/python; then \
		echo "SPD_CONF_ORIG_PATH=\"@spdconforigdir@\"" > speechd_config/paths.py; \
		echo "SPD_CONF_PATH=\"@spdconforigdir@\"" >> speechd_config/paths.py; \
		echo "SPD_SOUND_DATA_PATH=\"@spdsnddatadir@\"" >> speechd_config/paths.py; \
		./setup.py install --prefix=@prefix@; \
	else \
		echo "Python not found, not installing speechd module nor spd-conf"; \
	fi
	cp speechd_config/test.wav @snddatadir@

uninstall:
	rm -f @snddatadir@/test.wav

clean:
	rm -f *.pyc
	rm -rf build

distclean: clean

distdir:


check:

